class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        int n1=nums1.length(),n2=nums2.length();
        List<Integer> ans=new ArrayList<>();
        int i=0,j=0;
        while(i<n1 && j<n2){
            if(nums1[i]<nums2[j]){ans.add(nums1[i]); i++;}
            if(nums1[i]>nums2[j])(ans.add(nums[j]); j++;)


        }
        while()

        
    }
}
